
// initial state
// shape: [{ id, quantity }]
const state = {
  appName: 'Vuelixir'
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
